class BitIO():
    def __init__():
        self.input_count = 0
        self.current_char = get_next_char()
        self.is_ = None
        self.os_ = None
        self.output_char = 0
        self.output_index = 0
    def output_bit(bool bit):
        if bit & 1:
            output_char.flip_LSB
            output_char >> 1
            self.output_index += 1
        else:
            output_char >> 1
            self.output_index += 1


    def input_bit():
        if self.input_count == 8:
            self.input_count = 0
            self.current_char = get_next_char()
        bit = (self.current_char >> self.input_count) & 1
        self.input_count += 1
        return bit 

    def get_next_char():
        assert (f.get(c))
        return file.pop(char)
        (current_char << is_)